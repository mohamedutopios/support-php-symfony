{#
<turbo-frame id="formations">
    {% for formation in formations %}

        <div class="p-3 mb-4 bg-light shadow-sm rounded">
            <div class="d-flex align-items-center justify-content-between">

                <div>
                    <h5 class="mb-1">{{ formation.title }}</h5>
                    <p class="mb-0">
                        <strong>Duration:</strong> {{ formation.duration }} hours |
                        <strong>Start Date:</strong> {{ formation.startDate ? formation.startDate|date('d/m/Y') : 'Not specified' }} |
                        <strong>End Date:</strong> {{ formation.endDate ? formation.endDate|date('d/m/Y') : 'Not specified' }}
                    </p>
                </div>


                <div class="text-end">
                    <a href="{{ path('app_fomation_details', {id: formation.id}) }}"
                       data-turbo-frame="formation-details"
                       class="btn btn-info btn-lg">View</a>
                </div>
            </div>
        </div>
    {% else %}
        <p class="text-center text-muted">No records found</p>
    {% endfor %}

    <div class="mt-4 text-center">
        <a class="btn btn-success">Create New Formation</a>
    </div>
</turbo-frame>
#}

<turbo-frame id="formations">
    {% for formation in formations %}
        <div class="p-3 mb-4 bg-light shadow-sm rounded">
            <div class="d-flex align-items-center justify-content-between">
                <div>
                    <h5 class="mb-1">{{ formation.title }}</h5>
                    <p class="mb-1 text-muted">{{ formation.description }}</p>
                    <p class="mb-0">
                        <strong>Duration:</strong> {{ formation.duration }} hours |
                        <strong>Start Date:</strong> {{ formation.startDate ? formation.startDate|date('d/m/Y') : 'Not specified' }} |
                        <strong>End Date:</strong> {{ formation.endDate ? formation.endDate|date('d/m/Y') : 'Not specified' }}
                    </p>
                </div>
                <div class="text-end">
                    <a href="{{ path('app_formation_details', { id: formation.id }) }}"
                       data-turbo-frame="formation-details"
                       class="btn btn-info btn-lg">View</a>
                </div>
            </div>
        </div>
    {% else %}
        <p class="text-center text-muted">No records found</p>
    {% endfor %}
</turbo-frame>
